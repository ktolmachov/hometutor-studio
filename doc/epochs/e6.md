# Закрытые Итерации

Актуализировано по roadmap на **2026-04-19** (в т. ч. **ingest-acceleration**, **epoch-local-cors-defaults**, **epoch-ui-refactoring**, **epoch-metrics-decomposition**, **epoch-learning-route-continuity**; ранее **epoch-query-service-decomposition**, **epoch-adr-010-acceptance**, **epoch-user-state-decomposition**, **epoch-local-store-contracts**, **E29** quiz service boundary, **E28-A** US-8.2, **E16**, **E24-B-2**, **E11-R**, **E13**, **E12**, архив **T1–T25** и т. д.).

Этот файл хранит **исторические** детали закрытых итераций, чтобы не перегружать `tasklist.md`.
Источник правды по активному backlog и owner'ам — `doc/backlog_registry.yaml`; `doc/tasklist.md` — производный weekly view (регенерация: `scripts/backlog_registry_lint.py --sync-from-index --write-sync`).



## E6.1: Agent A — personalization policy + clamp (2026-04-08)

- `app/tutor_personalization_policy.py`: матрица подсказок + clamp решения оркестратора под due review / quiz emphasis.
- Проводка в `orchestrate_pedagogical_action_step`; `PedagogicalRouter` синхронно обогащает профиль; тесты `tests/test_tutor_personalization_policy.py`.

## E6.2: Agent B — tutor API/UI surfaces (2026-04-08)

- `query_service` / `api_models` / `tutor_learner_contract`: экспорт `tutor_orchestration_pipeline`, `tutor_pipeline`; KV snapshot.
- Streamlit: контекст тьютора + шаги пайплайна; тесты см. `changelog.md`.

## E6.2 follow-up: Agent B — typed payload cleanup + resume surfaces (2026-04-08)

- `query_service`: cleanup silent `except`; typed tutor payload стабилизирован для read-path'ов UI/API.
- `app/ui/resume_cards.py`: orchestration hints и tutor summary используют уже нормализованные поля, а не adhoc debug-путь.
- Документация изменений и проверочных тестов — в `doc/changelog.md`.

## E6.3: Agent C — persistence + architecture hardening (2026-04-08)

- `app/user_state.py`: WAL-режим, `synchronous=NORMAL`, write-lock и тестовый контракт на concurrent writes без потери данных.
- `app/adaptive_plan.py`: builder/model Adaptive Daily Plan вынесен из `learning_plan_service`, чтобы убрать циклическую связанность и закрепить один source of truth.
- Exception hygiene по owner-файлам `E6.3`: закрыт хвост с silent `except Exception: pass`; regression coverage — `tests/test_user_state.py`, `tests/test_learner_model_service.py`, `tests/test_learning_plan_service.py`.

## Активный следующий срез после закрытия E6.3 (2026-04-09)

- Исторически закрыты `E6.0–E6.3`.
- Текущий operational split в `doc/tasklist.md`: `E6.4 Agent A / tutor behavior quality`, `E6.5 Agent B / typed tutor read-paths + surfaces`, `E6.6 Agent C / tutor gate + CI runbook`.
- Этот файл хранит историю закрытых срезов; актуальные пакеты, owner-зоны и merge-order — `doc/backlog_registry.yaml` (SSoT); производный обзор — `doc/tasklist.md`.
