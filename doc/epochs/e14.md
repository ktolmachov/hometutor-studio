# Закрытые Итерации

Актуализировано по roadmap на **2026-04-19** (в т. ч. **ingest-acceleration**, **epoch-local-cors-defaults**, **epoch-ui-refactoring**, **epoch-metrics-decomposition**, **epoch-learning-route-continuity**; ранее **epoch-query-service-decomposition**, **epoch-adr-010-acceptance**, **epoch-user-state-decomposition**, **epoch-local-store-contracts**, **E29** quiz service boundary, **E28-A** US-8.2, **E16**, **E24-B-2**, **E11-R**, **E13**, **E12**, архив **T1–T25** и т. д.).

Этот файл хранит **исторические** детали закрытых итераций, чтобы не перегружать `tasklist.md`.
Источник правды по активному backlog и owner'ам — `doc/backlog_registry.yaml`; `doc/tasklist.md` — производный weekly view (регенерация: `scripts/backlog_registry_lint.py --sync-from-index --write-sync`).



## E14: Tutor Transparency Readability (2026-04-12)

- **E14-A / US-4.2 / CJM Trust:** всегда видимая плашка `render_tutor_transparency_badge()` в чате тьютора и read-only подписи `format_stored_orchestration_caption` / `tutor_orchestration_decision_one_liner` переводят фазу, источник решения, микро-квиз и причины policy clamp на короткий русский текст; известные коды (включая E11-R intent из `tutor_personalization_policy`) не показываются как snake_case в основной подписи; неизвестные коды деградируют в читаемую строку без подчёркиваний.
- **Guardrail:** без изменений оркестратора, API и router; только `app/ui/tutor_mastery_forecast_panel.py` и узкие unit-тесты.
- **Проверки:** `pytest tests/test_tutor_mastery_forecast_panel.py tests/test_e9_6_observable_surfaces.py`.
- Архив prompt: `archive/agent_prompts/e14_a_tutor_transparency_readability_2026-04-12.md`.

## E14-B/C: Arch Hardening (2026-04-12)

- **E14-B:** изоляция путей метрик в `Settings` (`metrics_store_path`, `metrics_dashboard_db_path`); `export_utils` / `event_tracking`; `flashcard_service` и `quiz_service` без импортов из `app/ui/`.
- **E14-C:** аудит `try/except` в `app/faq_memory.py`, `app/adaptive_plan.py`, `app/analytics_service.py`, `app/async_quality_judge.py`, `app/graph_retrieval.py` — все `except Exception` с `# noqa: BLE001` и однострочным обоснованием; осознанные catch-all (деградация без проброса) — лог **ERROR** и при необходимости `exc_info=True`; `run_step_safe` не использовался (контракт pipeline step не совпадает).
- **Проверки E14-C:** `pytest tests/test_faq_memory.py tests/test_analytics_service.py tests/test_async_quality_judge.py tests/test_graph_retrieval.py tests/test_concept_graduation.py tests/test_adaptive_plan_history.py tests/test_adaptive_plan_delta.py`.

## E14-D: Doc sweep (2026-04-12)

- **Scope:** только `doc/` — видимость runtime E12 Flashcards в `doc/architecture.md` и `doc/conventions_architecture.md`; **ADR-009** — статус `Deferred` (фактический каталог тем в `app/knowledge_service.py`, не HDBSCAN-at-reindex); § Тестирование в `doc/conventions_reference.md` (`pytest_configure` отключает reranker; патчи FAQ cache); запись — `doc/changelog.md` (2026-04-12).
