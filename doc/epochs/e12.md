# Закрытые Итерации

Актуализировано по roadmap на **2026-04-19** (в т. ч. **ingest-acceleration**, **epoch-local-cors-defaults**, **epoch-ui-refactoring**, **epoch-metrics-decomposition**, **epoch-learning-route-continuity**; ранее **epoch-query-service-decomposition**, **epoch-adr-010-acceptance**, **epoch-user-state-decomposition**, **epoch-local-store-contracts**, **E29** quiz service boundary, **E28-A** US-8.2, **E16**, **E24-B-2**, **E11-R**, **E13**, **E12**, архив **T1–T25** и т. д.).

Этот файл хранит **исторические** детали закрытых итераций, чтобы не перегружать `tasklist.md`.
Источник правды по активному backlog и owner'ам — `doc/backlog_registry.yaml`; `doc/tasklist.md` — производный weekly view (регенерация: `scripts/backlog_registry_lint.py --sync-from-index --write-sync`).



## E12: Flashcards + Persistent SRS (2026-04-11)

- **US-15.1–US-15.6:** персистентные колоды и карточки в SQLite (`flashcard_decks`, `flashcards` в `app/user_state.py`); генерация и SM-2 в `app/flashcard_service.py`; промпт `FLASHCARD_GENERATION_PROMPT` в `app/prompts.py`; HTTP — `app/routers/flashcards.py`, регистрация в `app/api.py`.
- **UI:** `app/ui/flashcards_ui.py` (колоды, создание, повторение); навигация и fragment — `app/ui/main.py`, `app/ui/fragments.py`; экспорт Anki в детали колоды через `st.cache_data` + `export_deck_to_anki` (без HTTP из UI).
- **Интеграция:** due-count и карточка Flashcards на home (`app/ui/resume_cards.py`); primary CTA при очереди flashcards ведёт на вкладку Flashcards → review (`guided_primary_home_cta_ru` + kind `flashcard_due` в `app/ui/continuity_bridge.py`); quiz → колода — `app/ui/interactive_quiz.py`.
- **Progress:** сводка «всего / освоено (interval > 21 дн.) / к повтору» — `get_flashcard_progress_stats()` в `user_state`, метрики на вкладке «Прогресс обучения» в `app/ui/dashboards.py`.
- **Проверки:** `pytest tests/test_flashcard_service.py` (CRUD/SM-2, парсинг JSON генерации, smoke API). Модель `FlashcardReviewRequest`: `quality` по умолчанию 4, чтобы работал запрос только с `quality_label`.
- Архитектурный план (вне репозитория): `C:\Users\educa\.claude\plans\shimmering-wobbling-backus.md`.
