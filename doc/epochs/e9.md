# Закрытые Итерации

Актуализировано по roadmap на **2026-04-19** (в т. ч. **ingest-acceleration**, **epoch-local-cors-defaults**, **epoch-ui-refactoring**, **epoch-metrics-decomposition**, **epoch-learning-route-continuity**; ранее **epoch-query-service-decomposition**, **epoch-adr-010-acceptance**, **epoch-user-state-decomposition**, **epoch-local-store-contracts**, **E29** quiz service boundary, **E28-A** US-8.2, **E16**, **E24-B-2**, **E11-R**, **E13**, **E12**, архив **T1–T25** и т. д.).

Этот файл хранит **исторические** детали закрытых итераций, чтобы не перегружать `tasklist.md`.
Источник правды по активному backlog и owner'ам — `doc/backlog_registry.yaml`; `doc/tasklist.md` — производный weekly view (регенерация: `scripts/backlog_registry_lint.py --sync-from-index --write-sync`).



## E9.7: Continuity & learn bridge (2026-04-10)

- Единый блок «Продолжить» на главной: `render_home_continue_unified` (`resume_cards.py`), приоритетные строки — `app/ui/continuity_bridge.py`; колонка слева — только вторичные действия тьютора (`render_resume_card`).
- Мост Q&A → тьютор: подпись `qa_to_tutor_bridge_caption_ru` в `query_tab.py` под CTA «Учить тему» / quiz.
- Превью сессии чата: `last_user_preview` в metadata при `session_store.save`, поле в `list_sessions`, формат подписи в `main.py`.
- Recovery после сбоя `/ask`: `ask_failure_recovery_hint_from_exception` в `helpers.py` + caption в `query_tab.py`.
- Due «одним взглядом»: `due_reviews_home_teaser_ru` в unified-блоке; детальный список — `render_due_reviews_card` справа.
- Проверки: `tests/test_e9_7_continuity_bridge.py`, расширенный `tests/test_session_store.py`.

## E9.6: Observable loop & ingest feedback (2026-04-10)

- Сводка reindex/ingest: `build_ingest_run_summary`, `ingest_run_summary` в `get_ingestion_status`, stdout `INGEST_SUMMARY`, polling в `query_tab` после `POST /reindex`.
- Медленный ответ: `answer_latency_bucket_label_ru` / `slow_answer_scope_hint_ru` и caption в `query_tab`.
- Решение оркестратора на поверхности тьютора: `tutor_orchestration_decision_one_liner` в `_render_tutor_chat_tab`.
- Progress hub MVP: `adaptive_plan_progress_teaser_caption` в блоке «Прогресс обучения».
- Graph-touch в trust-слое Q&A: `graph_expansion_trust_caption_ru` над карточками источников.
- Проверки: `tests/test_e9_6_observable_surfaces.py`.

## E9.5: Truth View & verifiable surfaces (2026-04-10)

- Провенанс источников Q&A: поля `route` и `rank_reason` в ответе API и в `retrieval_trace`, отображение в `app/ui/source_cards.py`; FAQ-cache обогащает те же поля (`faq_cache`).
- Латентность: подпись после ответа в `app/ui/query_tab.py` по `debug.total_answer_ms` (bucket быстро/нормально/долго).
- Discover: верхний блок позиционирования в корневом `README.md` + ссылка на `doc/product_idea.md`.
- Контракт: `tests/test_query_service.py`, `tests/test_source_cards_trust.py`, `tests/test_telegram_parsing.py`.
- Telegram `/help`: `TELEGRAM_HELP_TEXT` в `app/telegram_handlers.py`; границы бота vs Streamlit в `doc/user_guide.md`.

## E9.4: Ingest progress · plan history · restore & inline trust (2026-04-10)

- Прогресс первой индексации: `INGEST_PROGRESS` в `app/ingestion.py`, `ingest.py -y` / `--reset`; проверка `tests/test_ingestion_progress.py`.
- История adaptive plan: KV `adaptive_daily_plan_history_json`, expander в `adaptive_plan_card`; проверка `tests/test_adaptive_plan_history.py`.
- Restore wizard: `preview_full_sync_bundle` и мастер в sidebar; проверка `test_preview_full_sync_bundle_counts_and_rejects_version`.
- Inline-цитаты: `QA_PROMPT`, `cite_index`, `linkify_qa_inline_citations`, якоря в `source_cards`; проверка `tests/test_answer_citations.py`.
- Один явный путь установки: корневой `README.md` и `.env.example` с `OPENAI_API_KEY`.

## E9.3: Retention & env clarity (2026-04-10)

- Recovery spaced repetition: `defer_overdue_reviews_for_recovery`, KV `streamlit_last_active_iso`, кнопка в `render_due_reviews_card` при длинном overdue после перерыва.
- Очередь due как топ-N: caption в `render_due_reviews_card`.
- Первый шаг по плану: caption в `render_adaptive_plan_hub`.
- Нет `OPENAI_API_KEY`: `render_config_env_banner` в `main.py`.
- Backup обучения: подпись и `help` у `st.download_button` в sidebar.
- Проверки: `test_spaced_repetition_recovery`, `test_streamlit_activity_gap`.

## E9.2: Cold start & plan clarity (2026-04-10)

- Примеры вопросов в hero: `suggest_example_questions`, кнопки в `hero.py`, `question_draft` и вкладка «Быстрый ответ».
- Мгновенный разбор микро-квиза: `_micro_quiz_status_ru`, строка результата, `question_explanation` в `tutor_micro_quiz_active`.
- Подсказка без «штрафа»: caption под блоком подсказки в `main.py`.
- Прозрачность тьютора: `recommended_action` из KV в описании `render_tutor_transparency_badge`.
- Что изменилось в плане: `plan_concepts_delta`, `render_plan_concepts_delta_ui`.
- Проверки: `test_adaptive_plan_delta`, `test_hero_example_questions`, `test_query_tab_topic_infer`.

## E9.1: Trust & continuity (2026-04-10)

- Smart-default retrieval: эвристика `detect_extended_query_type` при `ENABLE_CLASSIFIER=false`; первый turn/cold ask переводит `vector_only` в `hybrid`, trace `smart_default_retrieval`.
- Причина приоритета due: `get_weak_concepts`, `get_recent_quiz_levels_low_score`, строка причины в `render_due_reviews_card`.
- Badge после reindex: `render_learner_profile_migration_badge` на вкладке «Прогресс обучения».
- Доверие к источнику: caption под «Источники» и подсказка по score в `source_cards`.
- Проверки: `test_resume_cards`, `test_source_cards_trust`, `test_learner_migration_badge`, `test_retrieval_profile`.

## Как читать вместе с tasklist

1. Для того, что делать сейчас: `tasklist.md`
2. Для того, как был закрыт tutor-path: этот файл
3. Если есть конфликт: верить `tasklist.md` и коду

## Owner decision: E4 sprint closure -> E5 horizon (2026-04-07)

- `doc/tasklist.md`: `E4 Graph horizon` зафиксирован как `closed` по sprint exit criteria после срезов `E4.0–E4.15`.
- `doc/future_roadmap.md`: активный следующий горизонт переведён на `E5` (`Personalization & Long-term Learner`).
- Возврат в `E4` допустим только как regression / maintenance exception, а не как основной active backlog.

## Owner decision: E5 delivery slice closed -> E6 horizon; E6.0 orchestration contract (2026-04-08)

- `doc/tasklist.md`: горизонт **E5 Learner** (E5.0–E5.8) — `closed`; Truth View / Now — активный **E6 Agentic Tutor**.
- `doc/future_roadmap.md`: текущий operational backlog — **E6** после закрытия E5-среза.
- **E6.0:** `app/tutor_pipeline_contract.py`, rule-fallback оркестратор, `trace["tutor_pipeline"]`, обновления `pipeline_steps` / `tutor_orchestrator` / `conventions.md` (детали — `changelog.md`).
