# E2-семпл разбора №26 «Гейт учебного контента» — 2026-07-19

Живой семпл генеративного контента на реальном мега-бандле пользователя
(`D:\AI\app\data`, генерация `reset_home_rag_v2_2026_07_17T21_01_09_656822_00_00`).
Правило гайда §4.1 соблюдено: рубрика зафиксирована **до** генерации, все выходы
сохранены, cherry-picking запрещён.

## Конфигурация (снимок до генерации)

| Параметр | Значение |
|---|---|
| Runtime | hometutor @ `84b7b5668` «343» |
| Модель | `qwopus3.6-35b-a3b-v1-mtp` @ `http://127.0.0.1:8080/v1` (llama.cpp; конфиг продукта) |
| Промпт | `QUIZ_SCOPED_PROMPT` / `SYNTHESIS_PROMPT` @ 343, T=0.25 (quiz) |
| Путь вызова | production-ветка `topic + documents`: `fetch_document_chunks_text` → `generate_scoped_quiz_from_content` (полный контекст реального вызывающего кода; catalog-ветка на живом бандле мертва — `get_topics_catalog()` → 0 тем) |
| Кэш | `LLM_REQUEST_CACHE_PERSIST=false`; один процесс на повтор (иначе in-process кэш дедуплицирует повторы) |
| Темы | `guardrails`, `agentic-loop`, `rag` — из 8 живых тем с конкурирующими источниками двух курсов |
| Семпл | 3 темы × 3 повтора = 9 quiz-генераций (54 вопроса) + 3 синтеза урока |

## Файлы

- `quiz_sample_r1.json` / `r2` / `r3` — полные записи: source_paths, контекст
  генерации (`context_text`), все вопросы, latency; ничего не отброшено.
- `lesson_synthesis_sample.json` — кандидат-уроки (`synthesize_topic`) по тем же
  темам: summary, секции, coverage (live-баг: `covered=2/total=0`).
- `rubric_scores.json` — детерминированный лексический скоринг по рубрике v1.0
  (D1–D6; диагностика для приоритизации, НЕ вердикт о качестве — см. ниже).
- [`semantic_adjudication_2026-07-19.md`](semantic_adjudication_2026-07-19.md) —
  **семантическая аджюдикация** (Claude, отдельная сессия от генерации, не
  self-grade): 13/13 D2-флагов адъюдицированы построчно против сырого
  `context_text`, 2 confirmed findings с цитатами и методическим выводом
  (лексический overlap не поймал самое опасное искажение — оно имело D2=1.0).

## Рубрика v1.0 (зафиксирована до генерации)

D1 evidence-поле в выходе · D2/D3 лексическая поддержка правильного ответа/стема
(диагностика, НЕ semantic entailment — правило контр-аудита №25) · D4 дубли ·
D5 распределение difficulty · D6 разнообразие источников контекста.
Семантические критерии (groundedness, педагогическая валидность, hallucination)
не авто-скорились; аджюдикация — независимой моделью (Claude), не self-grade.

## Итоги (механизм, не частота — семпл 3×3)

- D1: **0/54** вопросов несут evidence/source/origin-поле (схема не просит);
- D2 < 0.6: 13/54; D4 дубли: 0; D5: 21 recognition / 15 recall / 18 transfer;
- **два confirmed findings** (после ручной семантической аджюдикации, не по
  диагностике D1–D6 — см. `semantic_adjudication_2026-07-19.md`):
  - Finding 1 (`DISTORTED`) `r1/guardrails#3` — «Уголовное наказание для
    системы» из ASR-оговорки («…не присядешь… Ну, не мне, а системе»),
    структурно валиден, дошёл бы до mastery. Имел D2=1.0 — token overlap
    не поймал искажение;
  - Finding 2 (`BORDERLINE`) `r2/guardrails#4` — «главная проблема
    классической инфраструктуры» приписывает агентское поведение
    классической инфраструктуре (subject substitution при дословно
    верной фразе);
  - остальные 12/13 D2-флагов — `GROUNDED` (легитимный перефраз, ложное
    срабатывание лексической диагностики).
- контр-пример валидности: `r3/guardrails#3` «50 тысяч токенов на тулы MCP» —
  подтверждён контекстом дословно.

VLQR/SGAR-проценты по этому семплу **не заявляются** (нужно 5×3 c независимой
аджюдикацией — P1 плана). D3-only флаги (22 доп. пункта) не адъюдицированы
построчно — явный scope cut, не полнота.

## Replay

Post-ship: повторить тот же прогон (темы/бандл/рубрика) после P0
(`doc/next/course_content_gate_compiler_plan.md`); Finding 1 обязан не попасть
в mastery без ярлыка (детерминированный exact-match gate это закрывает);
Finding 2 честно может повториться — subject-substitution класс требует
semantic entailment слоя, вне P0 (offline/P2).
